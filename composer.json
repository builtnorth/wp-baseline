{
    "name": "builtnorth/core",
    "description": "Core functionality that is used on BuiltNorth WordPress sites. Adds security and hardening features and cleans up some default functionality.",
    "type": "library",
    "license": "GPL-2.0-or-later",
    "authors": [
        {
            "name": "Built North",
            "homepage": "https://builtnorth.co"
        }
    ],
    "require": {
        "php": ">=8.1",
        "composer/installers": "~1.0 || ~2.0"
    },
  	"autoload": {
        "files": [
            "inc/cleanup.php",
            "inc/security.php"
        ]
    },
    "config": {
        "allow-plugins": {
            "composer/installers": true
        }
    }
}
